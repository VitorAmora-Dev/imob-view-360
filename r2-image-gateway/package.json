{
  "name": "arp-vision-r2-image-gateway",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "test:runtime": "node --test integration/runtime.mjs",
    "build": "wrangler deploy --dry-run --outdir dist",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "miniflare": "^4.0.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "sharp": "0.35.4",
    "undici": "7.29.0"
  }
}
